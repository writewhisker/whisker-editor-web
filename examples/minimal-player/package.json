{
  "name": "@whisker-examples/minimal-player",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Minimal story player example using Whisker",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "deploy": "npm run build && netlify deploy --prod"
  },
  "dependencies": {
    "@writewhisker/core-ts": "workspace:*",
    "@writewhisker/player-ui": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
