{
  "name": "@whisker-examples/full-editor",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Full editor example using Whisker",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "type-check": "svelte-check",
    "deploy": "npm run build && netlify deploy --prod"
  },
  "dependencies": {
    "@writewhisker/editor-base": "workspace:*",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
